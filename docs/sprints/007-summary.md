# Sprint 7 — summary

(Pendente) Reorganizar raiz: remover legado (`index.html`, `app.js`, `styles.css`), mover Go para `cmd/server` + `internal/`, gitignore de binários locais. `public/downloads/` permanece versionado.
