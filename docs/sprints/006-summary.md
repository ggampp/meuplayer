# Sprint 6 — summary

Documentação reorganizada em `docs/`. README reflete servidor Go. Estilos divididos em partials CSS. Handlers HTTP extraídos de `main.go`. `public/downloads/` permanece versionado. Próximo passo: modularizar `app.jsx`.
