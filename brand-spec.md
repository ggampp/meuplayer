# MeuPlayer — assets e identidade

- Logo existente: `public/favicon-32x32.png`; ícone maior: `public/apple-touch-icon.png`.
- Pôsteres e cenários reais: proxy existente `/api/image/tmdb/`, carregados a partir dos metadados do catálogo.
- Paleta: tokens `--color-paper`, `--color-ink`, `--color-accent` em `public/tokens.css`.
- Tipografia: Segoe UI Variable, Segoe UI, sans-serif; fontes locais, sem nova dependência externa.
- Interface implementada: `/`, `/vod`, `/tv`. As formas abstratas na entrada identificam experiências; não representam produtos ou títulos reais.
